// for(initialzation ; condition ; increment/decrement){
//     statements
// }

// Reversing a string in Js

var str = 'nikhil'

console.log(str.split('').reverse().join('')) // Js doesn't have a direct .reverse on strings, so we need to convert them to an array and the reverse


